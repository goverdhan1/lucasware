'use strict';// The Domain on which lucas-rest-api is hosted on//amdApp.value('DomainPath', '${RestEndpoint}');//amdApp.value('DomainPath', 'http://local-lucas-api.com:8080');amdApp.value('DomainPath', 'http://localhost:8080');//amdApp.value('DomainPath', 'http://172.16.1.104:8080');//amdApp.value('DomainPath', 'http://lucas-api-dev.elasticbeanstalk.com');//The server api-root//amdApp.value('ApiPath', '${ApiPath}');amdApp.value('ApiPath', '/lucas-api');//The client path - Uncomment this before checking in//amdApp.value('ClientPath', '${LocalHost}');src//amdApp.value('JsonPath', '/app/localjsons');//The client path - use this for local development when pointing tc to /main/webapp/appamdApp.value('ClientPath', 'http://localhost:8080');amdApp.value('JsonPath', '/amd/localjsons');//amdApp.value('ClientPath', 'http://localhost:8080/src/main/webapp');//Setting this flag to true, results in by-passing the server completely//and uses the local json files for all rest calls.//Setting this to false, uses a mix of server/local versions based on individual flag values (ref: RestApiService.js)amdApp.value('UseLocalJsons', true);